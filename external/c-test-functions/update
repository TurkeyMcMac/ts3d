#!/bin/sh

base_url=https://raw.githubusercontent.com/TurkeyMcMac/c-test-functions/master

fetch() {
	mkdir -p "`dirname "$1"`"
	curl -s "${base_url}/$1" > "./$1" || exit 1
}

fetch README &
fetch lib/libctf.h &
wait

cd ../../src
ln -fs ../external/c-test-functions/lib/libctf.h
